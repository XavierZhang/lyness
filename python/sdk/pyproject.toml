[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "lyness-sdk"
version = "0.0.0.dev0"
description = "Python SDK for lyness"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "DeepSeek" }]
dependencies = [
  "pydantic>=2.12,<3",
  "lyness-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/XavierZhang/lyness"
Documentation = "https://github.com/XavierZhang/lyness/blob/master/docs/user/guide/python-sdk.md"
Issues = "https://github.com/XavierZhang/lyness/issues"
Source = "https://github.com/XavierZhang/lyness"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/deepseek_harness"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
lyness-runtime-bin = { path = "../sdk-runtime", editable = true }
