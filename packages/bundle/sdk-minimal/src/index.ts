/**
 * @lyness/lyn-sdk-minimal — the standalone minimal SDK profile bundle.
 * The package's substance is `cordis.patch.yml`, declared by the
 * `lyn.bundle.patch` manifest field and resolved by the profile composer;
 * this module carries no runtime interface.
 * @module @lyness/lyn-sdk-minimal
 */

export {}
