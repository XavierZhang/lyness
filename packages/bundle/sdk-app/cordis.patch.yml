# The SDK application over lyn-base. Stdout belongs exclusively to JSON-RPC.

- id: system-prompt
  config:
    personaSuffix: Your working directory is {{cwd}}.
    personaPrefix: >-
      You are a coding agent powered by the {{model}} model.

- id: session-title-llm
  disabled: true

- insert:
    - id: sdk-app-startup
      name: '@lyness/lyn-sdk-app'
      config:
        profile: sdk

    - id: sdk-jsonrpc-server
      name: '@lyness/lyn-sdk-jsonrpc-server'
      inject: [sdkAppStartup, loader]
      config:
        maxTokensAsSuccess: !!js "process.env.LYNESS_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.LYNESS_MAX_TOKENS_AS_SUCCESS)"

- id: hmr
  disabled: true

# The packaged carrier supplies a default; LYNESS_PRIMARY_RUNTIME overrides it,
# including an empty-string opt-out. Each row can be patched independently.
- insert:
    - id: workspace-dependencies
      name: '@lyness/lyn-tool-workspace-dependencies'
      disabled: !!js "!(process.env.LYNESS_PRIMARY_RUNTIME ?? process.env.LYNESS_BUNDLED_PRIMARY_RUNTIME)"
      config:
        source: !!js "process.getBuiltinModule('node:path').resolve(process.env.LYNESS_PRIMARY_RUNTIME ?? process.env.LYNESS_BUNDLED_PRIMARY_RUNTIME ?? '')"

    - id: skill-office
      name: '@lyness/lyn-skill-office'
      disabled: !!js "!(process.env.LYNESS_PRIMARY_RUNTIME ?? process.env.LYNESS_BUNDLED_PRIMARY_RUNTIME)"
      config:
        assetRoot: !!js "process.getBuiltinModule('node:path').resolve(process.env.LYNESS_PRIMARY_RUNTIME ?? process.env.LYNESS_BUNDLED_PRIMARY_RUNTIME ?? '', '..', 'office-skills')"
        node: !!js "process.getBuiltinModule('node:path').resolve(process.env.LYNESS_PRIMARY_RUNTIME ?? process.env.LYNESS_BUNDLED_PRIMARY_RUNTIME ?? '', 'dependencies', 'node', 'bin', process.platform === 'win32' ? 'node.exe' : 'node')"
