- insert:
    - id: mcp-pagination-limit
      name: '@lyness/lyn-mcp-client'
      config:
        transport: stdio
        serverName: pagination-limit
        command: !!js process.execPath
        args: !!js '[process.env.LYNESS_MCP_PAGINATION_FIXTURE]'
        failOnStartupError: true
        reconnect:
          enabled: false
