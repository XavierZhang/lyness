/**
 * @lyness/lyn-experimental-agent-team-profile — experimental Agent Teams profile bundle.
 * The package's runtime content is its `lyn.bundle.patch` document; this
 * module exports no runtime API.
 * @module @lyness/lyn-experimental-agent-team-profile
 */

export {}
