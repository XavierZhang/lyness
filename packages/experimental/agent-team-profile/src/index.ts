/**
 * @lyness/experimental-agent-team-profile — private Agent Teams profile bundle.
 * The package's runtime content is its `lyn.bundle.patch` document; this
 * module exports no runtime API.
 * @module @lyness/experimental-agent-team-profile
 */

export {}
