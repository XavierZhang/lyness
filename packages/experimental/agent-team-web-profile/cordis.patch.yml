# Experimental Agent Teams Web layer. Apply after lyn-web-app and the host-side
# lyn-agent-team-profile so the browser mounts only when the Team service is present.

- insert:
    - id: ui-agent-team
      name: '@lyness/experimental-client-ui-agent-team'
