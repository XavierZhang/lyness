# Test-only SDK provider patch over the shipped headless profile. The child
# is a complete sdk profile; the parent omits `cwd` to exercise inheritance.
- id: headless-startup
  disabled: true

- id: headless-runner
  disabled: true

- id: agent-loop
  config:
    agents:
      - id: main
        provider: mock
        model: mock-delegate
        cwd: !!js process.cwd()

- id: session-persistence-jsonl
  config:
    root: './.sessions'
    compression: none

- id: tool-subagent
  disabled: true

- insert:
    - id: mock-llm
      name: './mock-delegating-llm.ts'

    - id: subagent-model-selection-settings
      name: '@lyness/lyn-tool-subagent/model-selection-settings'
      config:
        enabled: true
        allowedModels:
          - provider: mock
            model: mock-routed

    - id: test-tool-subagent
      name: './scoped-tool-subagent.ts'
      config:
        provider: lyn-sdk
        toolName: subagent
        agentOptions:
          maxTokens: 777
        maxDepth: 'provider-managed'

    # providerName is omitted to exercise the shipped `lyn-sdk` default.
    - id: subagent-lyn-sdk
      name: '@lyness/lyn-subagent-lyn-sdk'
      config:
        profile: sdk
        patches: !!js JSON.parse(process.env.LYNESS_TEST_CHILD_PATCHES ?? '[]')
        lynHome: !!js process.env.LYNESS_TEST_CHILD_HOME
        provider: mock
        model: mock-routed
        env:
          LYNESS_TELEMETRY_DISABLED: '1'
          LYNESS_TEST_CHILD_FAILURE: !!js String(process.env.LYNESS_TEST_CHILD_FAILURE ?? '')
