# Keyless generic ACP v1 control-surface patch over `lyn --profile acp`.

- id: llm-deepseek
  name: '@lyness/llm-deepseek'
  disabled: true

- id: acp
  name: '@lyness/acp'
  config:
    provider: control-fixture
    model: alpha

- id: session-persistence-jsonl
  name: '@lyness/session-persistence-jsonl'
  config:
    root: !!js process.env.LYNESS_CONFORMANCE_PERSISTENCE_ROOT
    compression: none

- insert:
    - id: control-surface-llm
      name: './control-surface-llm.ts'
