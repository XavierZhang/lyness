# Replay-only patch layered after `cordis.yml`: replace the network adapter
# with recorded JSONL, pin the recorded top-level model, and keep persistence
# raw for fixture harvesting.

- id: llm-deepseek
  name: '@lyness/lyn-llm-deepseek'
  disabled: true

- id: acp
  name: '@lyness/lyn-acp'
  config:
    provider: deepseek-official
    model: deepseek-v4-flash

- id: session-persistence-jsonl
  name: '@lyness/lyn-session-persistence-jsonl'
  config:
    root: !!js process.env.LYNESS_SNAPSHOT_SESSIONS_ROOT ?? lynHomePath('sessions')
    compression: none

- id: sandbox
  name: '@lyness/lyn-sandbox-local'
  config:
    runnerCommand:
      - bash
      - -c
      - while [ "$1" != "--" ]; do shift; done; shift; exec "$@"
      - passthrough-runner
    runnerFailureSignatures:
      - 'passthrough-runner: profile rejected'

- insert:
    - id: llm-replay
      name: '@lyness/lyn-llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
              - id: deepseek-v4-pro

    - id: subagent-settlement-marker
      name: './subagent-settlement-marker.ts'

- insert:
    - id: snapshot-shell-path
      name: ../../../scripts/snapshot-shell-path.ts
      config:
        command: "printf 'escalated\\n' > /tmp/lyn-escalated.txt && cat /tmp/lyn-escalated.txt && rm /tmp/lyn-escalated.txt"
        recordedPath: /tmp/lyn-escalated.txt
        livePath: !!js process.env.LYNESS_SNAPSHOT_SPILL_ROOT + '/lyn-escalated.txt'
