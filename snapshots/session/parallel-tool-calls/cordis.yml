# Filesystem-scenario overlay: the sandboxed filesystem stack already lives in
# `lyn-base`, so this patch changes only the local tool-result spill
# storage those scenarios exercise.

- id: spill-local
  name: '@lyness/spill-local'
  config:
    root: !!js process.env.LYNESS_SNAPSHOT_SPILL_ROOT ?? './.spill'

- id: spill-policy
  name: '@lyness/spill-policy'
  config:
    maxInlineBytes: !!js process.env.LYNESS_SNAPSHOT && 800 || 50000
