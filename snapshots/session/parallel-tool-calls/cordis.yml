# Filesystem-scenario overlay: the sandboxed filesystem stack already lives in
# `lyn-base`, so this patch changes only the local tool-result spill
# storage those scenarios exercise.

- id: spill-local
  name: '@lyness/lyn-spill-local'
  config:
    root: !!js process.env.LYNESS_SNAPSHOT_SPILL_ROOT ?? './.spill'

- id: spill-policy
  name: '@lyness/lyn-spill-policy'
  config:
    maxInlineBytes: !!js process.env.LYNESS_SNAPSHOT && 800 || 50000

- insert:
    - id: snapshot-spill-locators
      name: ../../../scripts/snapshot-spill-locators.ts
      config:
        root: !!js process.env.LYNESS_SNAPSHOT_SPILL_ROOT
        locatorRoot: !!js process.env.LYNESS_SNAPSHOT_SPILL_LOCATOR_ROOT
