# Live-recording patch shared by the ordinary headless snapshot composition.
# The shipped profile owns application startup; this patch fixes only the
# deterministic test composition and raw persistence needed for recording.

- id: llm-deepseek
  name: '@lyness/lyn-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max
    models:
      - id: deepseek-v4-flash
      - id: deepseek-v4-pro
      - id: deepseek-v4-flash-vision-exp
        inputModalities: [text, image]

- id: sandbox-policy
  name: '@lyness/lyn-sandbox-policy'
  config:
    mode: !!js "process.env.LYNESS_PERMISSION_MODE ?? (process.env.LYNESS_SNAPSHOT === undefined ? 'workspace-write' : 'danger-full-access')"
    workspaceRoot: !!js process.cwd()

- id: approval
  name: '@lyness/lyn-user-approval'
  config:
    policy: !!js "(process.env.LYNESS_PERMISSION_MODE ?? (process.env.LYNESS_SNAPSHOT === undefined ? 'workspace-write' : 'danger-full-access')) === 'danger-full-access' ? 'never' : 'ask'"

- id: session-persistence-jsonl
  name: '@lyness/lyn-session-persistence-jsonl'
  config:
    root: !!js lynHomePath('sessions')
    compression: none

- id: session-title-llm
  disabled: true

- id: system-prompt
  name: '@lyness/lyn-system-prompt'
  config:
    personaPrefix: |
      You are a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}. Your bash tool runs under a file sandbox — a `[sandbox: file access denied …]` result is policy, not a command bug.

      Verify your work by running the code or tests. Keep answers brief and factual.

- id: agent-instructions
  name: '@lyness/lyn-agent-instructions'
  config:
    maxBytes: 65536

- id: tool-subagent
  name: '@lyness/lyn-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    backgroundMode: continuable
    maxDepth: 1

- id: tool-subagent-fork
  name: '@lyness/lyn-tool-subagent'
  config:
    provider: fork
    toolName: subagent_fork
    backgroundMode: one-shot
    enableRunInBackground: false
    maxDepth: 1

- id: fs-sandbox
  name: '@lyness/lyn-fs-sandbox'
  config:
    cwd: !!js process.cwd()

- insert:
    - id: hooks-claude-code
      name: '@lyness/lyn-hooks-claude-code'
      config:
        configPath: ./hooks.json

    - id: hooks-codex
      name: '@lyness/lyn-hooks-codex'
      config:
        configPath: ./codex-hooks.json
