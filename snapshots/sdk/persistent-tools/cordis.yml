# Per-launch SDK customization: retain only persistent Bash and the string
# editor as model-facing tools, with process-local providers and an explicit
# danger-full-access / never-ask policy.

- id: llm-deepseek
  name: '@lyness/llm-deepseek'
  config:
    apiKeyEnv: DEEPSEEK_API_KEY
    streamIdleTimeoutMs: 172800000
    models:
      - id: !!js process.env.LYNESS_MODEL ?? 'deepseek-v4-flash'
        contextWindow: !!js Number(process.env.LYNESS_CONTEXT_WINDOW ?? 1000000)

- id: sandbox-policy
  name: '@lyness/sandbox-policy'
  config:
    mode: danger-full-access
    workspaceRoot: !!js process.cwd()

- id: approval
  name: '@lyness/user-approval'
  config:
    policy: never

- id: permission
  name: '@lyness/permission-presets'
  disabled: true

- id: bash-sandbox
  name: '@lyness/bash-sandbox'
  disabled: true

- id: pwsh-sandbox
  name: '@lyness/pwsh-sandbox'
  disabled: true

- id: fs-sandbox
  name: '@lyness/fs-sandbox'
  disabled: true

- id: tool-bash
  name: '@lyness/tool-bash'
  disabled: true

- id: tool-pwsh
  name: '@lyness/tool-pwsh'
  disabled: true

- id: agent-instructions
  name: '@lyness/agent-instructions'
  disabled: true

- id: skill
  name: '@lyness/skill'
  disabled: true

- id: skill-filesystem
  name: '@lyness/skill-filesystem'
  disabled: true

- id: tool-skill
  name: '@lyness/tool-skill'
  disabled: true

- id: tool-jobs
  name: '@lyness/tool-jobs'
  disabled: true

- id: tool-fs
  name: '@lyness/tool-fs'
  disabled: true

- id: tool-fs-search
  name: '@lyness/tool-fs-search'
  disabled: true

- id: tool-subagent-control
  name: '@lyness/tool-subagent-control'
  disabled: true

- id: tool-subagent-list-agents
  name: '@lyness/tool-subagent-control/list-agents'
  disabled: true

- id: tool-subagent
  name: '@lyness/tool-subagent'
  disabled: true

- id: tool-subagent-fork
  name: '@lyness/tool-subagent'
  disabled: true

- id: tool-subagent-report
  name: '@lyness/tool-subagent-report'
  disabled: true

- id: tool-workflow
  name: '@lyness/tool-workflow'
  disabled: true

- id: tool-todo
  name: '@lyness/tool-todo'
  disabled: true

- id: tool-goal
  name: '@lyness/tool-goal'
  disabled: true

- id: tool-ralph
  name: '@lyness/tool-ralph'
  disabled: true

- id: tool-web
  name: '@lyness/tool-web'
  disabled: true

- id: plan-mode
  name: '@lyness/plan-mode'
  disabled: true

- id: system-prompt
  name: '@lyness/system-prompt'
  config:
    includeHarnessIdentity: false
    persona: !!js process.env.LYNESS_SYSTEM_PROMPT ?? 'You are a helpful software engineer assistant.'

- id: session-persistence-jsonl
  name: '@lyness/session-persistence-jsonl'
  config:
    root: !!js lynHomePath('sessions')
    compression: none

- insert:
    - id: bash-local
      name: '@lyness/bash-local'

    - id: fs-local
      name: '@lyness/fs-local'
      config:
        cwd: !!js process.cwd()

    - id: terminal
      name: '@lyness/terminal'

    - id: terminal-bash
      name: '@lyness/terminal-bash'
      config:
        timeoutMs: 300000

    - id: persistent-bash
      name: '@lyness/tool-bash-persistent'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * You do have access to a mirror of common linux and python packages via apt and pip.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.
