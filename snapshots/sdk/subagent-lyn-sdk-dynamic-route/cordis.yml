# SDK-profile patch for a deterministic parent model that selects a route for
# a separate SDK child runtime. Both runtimes persist their request headers.

- id: llm-deepseek
  name: '@lyness/llm-deepseek'
  disabled: true

- id: tool-subagent
  name: '@lyness/tool-subagent'
  disabled: true

- id: sdk-jsonrpc-server
  name: '@lyness/sdk-jsonrpc-server'
  disabled: true

- id: session-persistence-jsonl
  name: '@lyness/session-persistence-jsonl'
  config:
    root: !!js lynHomePath('sessions')
    compression: none

- insert:
    - id: mock-llm
      name: '../../../packages/subagent/subagent-lyn-sdk/tests/fixtures/loader/mock-delegating-llm.ts'
      disabled: !!js process.env.LYNESS_SNAPSHOT !== 'record'

    - id: subagent-lyn-sdk
      name: '@lyness/subagent-lyn-sdk'
      config:
        profile: sdk
        patches: !!js JSON.parse(process.env.LYNESS_TEST_CHILD_PATCHES ?? '[]')
        lynHome: !!js process.env.LYNESS_TEST_CHILD_HOME
        provider: mock
        model: mock-routed
        env:
          LYNESS_TELEMETRY_DISABLED: '1'

    - id: subagent-model-selection-settings
      name: '@lyness/tool-subagent/model-selection-settings'
      config:
        enabled: true
        allowedModels:
          - provider: mock
            model: mock-routed

    - id: tool-subagent-lyn-sdk
      name: '../../../packages/subagent/subagent-lyn-sdk/tests/fixtures/loader/scoped-tool-subagent.ts'
      config:
        provider: lyn-sdk
        toolName: subagent
        enableRunInBackground: false
        agentOptions:
          maxTokens: 777
        maxDepth: 'provider-managed'

    - id: sdk-jsonrpc-server-dynamic-live
      name: '@lyness/sdk-jsonrpc-server'
      inject: [sdkAppStartup, loader]
      disabled: !!js process.env.LYNESS_SNAPSHOT !== 'record'
      config:
        maxTokensAsSuccess: true
