# SDK-profile patch for the deterministic diagnostic child runtime.

- id: llm-deepseek
  name: '@lyness/llm-deepseek'
  disabled: true

- id: agent-instructions
  name: '@lyness/agent-instructions'
  disabled: true

- id: system-prompt
  name: '@lyness/system-prompt'
  config:
    persona: 'Return the scripted LYN SDK failure.'

- insert:
    - id: child-mock-llm
      name: '../../../packages/subagent/subagent-lyn-sdk/tests/fixtures/loader/child-mock-llm.ts'
