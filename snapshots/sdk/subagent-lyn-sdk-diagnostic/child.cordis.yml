# SDK-profile patch for the deterministic diagnostic child runtime.

- id: llm-deepseek
  name: '@lyness/lyn-llm-deepseek'
  disabled: true

- id: agent-instructions
  name: '@lyness/lyn-agent-instructions'
  disabled: true

- id: system-prompt
  name: '@lyness/lyn-system-prompt'
  config:
    personaPrefix: 'Return the scripted LYN SDK failure.'

- insert:
    - id: child-mock-llm
      name: '../../../packages/subagent/subagent-lyn-sdk/tests/fixtures/loader/child-mock-llm.ts'
